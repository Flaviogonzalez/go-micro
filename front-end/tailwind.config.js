/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./**/*.html",      // Archivos HTML
    "./**/*.gohtml",    // Si usás plantillas Go
    "./**/*.tmpl",      // Si usás otra extensión de plantillas
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
